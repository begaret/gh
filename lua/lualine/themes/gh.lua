local colors = require('gh.colors')

return {
	normal = {
		a = { bg = colors.white, fg = colors.black, gui = 'bold' },
		b = { bg = colors.gray1, fg = colors.white },
		c = { bg = colors.gray1, fg = colors.white },
	},

	insert = {
		a = { bg = colors.white, fg = colors.black, gui = 'bold' },
		b = { bg = colors.gray1, fg = colors.white },
		c = { bg = colors.gray1, fg = colors.white },
	},

	command = {
		a = { bg = colors.white, fg = colors.black, gui = 'bold' },
		b = { bg = colors.gray1, fg = colors.white },
		c = { bg = colors.gray1, fg = colors.white },
	},

	visual = {
		a = { bg = colors.white, fg = colors.black, gui = 'bold' },
		b = { bg = colors.gray1, fg = colors.white },
		c = { bg = colors.gray1, fg = colors.white },
	},

	replace = {
		a = { bg = colors.white, fg = colors.black, gui = 'bold' },
		b = { bg = colors.gray1, fg = colors.white },
		c = { bg = colors.gray1, fg = colors.white },
	},

	terminal = {
		a = { bg = colors.white, fg = colors.black, gui = 'bold' },
		b = { bg = colors.gray1, fg = colors.white },
		c = { bg = colors.gray1, fg = colors.white },
	},

	inactive = {
		a = { bg = colors.white, fg = colors.black, gui = 'bold' },
		b = { bg = colors.gray1, fg = colors.white },
		c = { bg = colors.gray1, fg = colors.white },
	}
}

